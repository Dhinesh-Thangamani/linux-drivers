sudo mkdir -p /opt/reboot-cycle

//copy reboot_cycle.sh to /opt/reboot-cycle/
// Change to executable
sudo chmod +x /opt/reboot-cycle/reboot_cycle.sh

// create systemd service
//sudo nano /etc/systemd/system/reboot-cycle.service
//copy the file reboot-cycle.service to /etc/systemd/system/
// Reload the system
sudo systemctl daemon-reload

// Enable
sudo systemctl enable reboot-cycle.service

// Start the reboot cycle
sudo systemctl start reboot-cycle.service

// Count check
cat /opt/reboot-cycle/count

// Log Check
cat /opt/reboot-cycle/reboot.log


